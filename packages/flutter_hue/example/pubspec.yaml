name: example
description: Demonstrates how to use the flutter_hue package.
publish_to: "none"
version: 1.2.4

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hue:
    path: ../
  cupertino_icons: ^1.0.2
  uni_links: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
