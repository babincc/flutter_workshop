name: radio_group_v2
description: A widget that groups radio buttons so they can work together to give the user a pleasant experience when making selections within the app.
version: 2.1.0
repository: https://github.com/babincc/flutter_workshop/tree/master/packages/radio_group_v2

environment:
  sdk: '>=2.12.0 <3.0.0'
  # flutter: ">=0.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
flutter:
